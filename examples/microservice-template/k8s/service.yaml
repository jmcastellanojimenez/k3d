apiVersion: v1
kind: Service
metadata:
  name: ecotrack-service
  namespace: ecotrack-dev
  labels:
    app: ecotrack-service
    service: ecotrack-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: ecotrack-service